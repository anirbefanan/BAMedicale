const test=require("node:test"),assert=require("node:assert/strict"),os=require("../../jumi/content-os.js");
const now=Date.parse("2026-09-21T12:00:00+07:00");
test("Content OS artwork locks accept only the required ratios",()=>{assert.equal(os.artwork("Article",1600,900).valid,true);assert.equal(os.artwork("Article",900,1600).valid,false);assert.equal(os.artwork("eBook",900,1200).valid,true);assert.equal(os.artwork("Seminar",707,1000).valid,true);});
test("Article intelligence separates audience, content type, tags, and hashtags",()=>{const result=os.recommendArticle({title:"Clinical diagnosis and treatment guide",quickSummary:"A practical clinician guide.",tags:["Thyroid","Oncology"]});assert.equal(result.primaryAudience,"Doctors");assert.equal(result.contentType,"Educational Guide");assert.deepEqual(result.tags,["Thyroid","Oncology"]);assert.deepEqual(result.socialKit.hashtags,["#Thyroid","#Oncology"]);});
test("ambiguous Article evidence remains Needs Review",()=>{const result=os.recommendArticle({title:"An overview"});assert.equal(result.status,"Needs Review");assert.equal(result.primaryAudience,"");});
test("dashboard comparisons use Jakarta WoW, elapsed MTD, and completed months",()=>{const data={content:[{id:"a",type:"Article",status:"Published",publishedAt:"2026-09-21T08:00:00+07:00"},{id:"b",type:"eBook",status:"Published",publishedAt:"2026-08-20T08:00:00+07:00"},{id:"c",type:"Article",status:"Published",publishedAt:"2026-08-05T08:00:00+07:00"},{id:"d",type:"Article",status:"Published",publishedAt:"2026-07-05T08:00:00+07:00"}],events:[]};const result=os.dashboard(data,now);assert.equal(result.comparison.wow.current,1);assert.equal(result.comparison.mtd.current,1);assert.equal(result.comparison.mtd.previous,2);assert.equal(result.comparison.month.current,2);assert.equal(result.comparison.month.previous,1);});
test("dashboard reports cadence and attention without fabricated insights",()=>{const result=os.dashboard({content:[{id:"x",type:"Article",title:"Review",status:"Validation Required",updatedAt:"2026-09-21T08:00:00+07:00"}],events:[]},now);assert.equal(result.attention.length,1);assert.equal(result.weeklyTypes.find(x=>x.type==="Article").met,false);assert.ok(result.insights.every(Boolean));});
