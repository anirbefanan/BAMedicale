const fs = require('node:fs');
const path = require('node:path');
const crypto = require('node:crypto');
const escape = (value = '') => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const hash = file => crypto.createHash('sha256').update(fs.readFileSync(file)).digest('hex');
module.exports = ({ presentation: p, event, root, domain }) => {
  const source = JSON.parse(fs.readFileSync(path.join(root, p.sourceManifest), 'utf8'));
  if (hash(path.join(root, p.sourcePdf)) !== source.sourceSha256 || hash(path.join(root, p.video)) !== source.videoSha256) throw new Error(`${p.id}: original source hash mismatch`);
  if (source.pages.length !== 20 || source.pages.some((s, i) => s.page !== i + 1 || !fs.existsSync(path.join(root, s.image)))) throw new Error(`${p.id}: incomplete source pages`);
  const url = `${domain}/${p.canonicalUrl}`;
  const asset = value => `../${value}`;
  const repeated = new Map();
  source.pages.forEach(s => s.figures.forEach(f => repeated.set(f.src, (repeated.get(f.src) || 0) + 1)));
  const schema = { '@context':'https://schema.org', '@type':'PresentationDigitalDocument', name:p.title, description:p.summary, author:{'@type':'Person',name:p.author.name,url:`${domain}/dr-bob-profile.html`}, datePublished:p.publishedDate, url, inLanguage:'en', isBasedOn:`${domain}/${p.sourcePdf}`, encoding:{'@type':'MediaObject',contentUrl:`${domain}/${p.sourcePdf}`,encodingFormat:'application/pdf'}, isPartOf:{'@type':'Event',name:event.title,startDate:event.startDate,url:`${domain}/${event.detailUrl}`} };
  const download = `<a class="button button-outline" href="${asset(p.sourcePdf)}" download>Download Original PDF</a>`;
  return `<!DOCTYPE html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escape(p.title)} | ${escape(p.author.name)} | BA Medicale</title><meta name="description" content="${escape(p.summary)}"><meta name="author" content="${escape(p.author.name)}"><link rel="canonical" href="${url}"><meta property="og:type" content="article"><meta property="og:title" content="${escape(p.title)}"><meta property="og:description" content="${escape(p.summary)}"><meta property="og:url" content="${url}"><meta property="og:image" content="${domain}/${p.cover}"><meta property="og:image:alt" content="Original presentation title slide"><meta property="og:site_name" content="BA Medicale"><meta name="twitter:card" content="summary_large_image"><link rel="icon" href="/favicon.ico"><link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&amp;family=Source+Serif+4:wght@400;500;600;700&amp;display=swap" rel="stylesheet"><link rel="stylesheet" href="../styles.css?v=immersive-20260902"><link rel="stylesheet" href="../presentation.css"><script type="application/ld+json">${JSON.stringify(schema).replace(/</g,'\\u003c')}</script></head>
<body class="route-article route-presentation"><div data-shell></div><main class="seo-article-page"><nav class="seo-breadcrumb" aria-label="Breadcrumb"><a href="../library.html">Medical Library</a><span>/</span><a href="../${event.detailUrl}">Seminar · 19 September 2026</a><span>/</span><span>Presentation</span></nav><article><header class="seo-article-hero seo-article-hero--publication"><div class="seo-article-heading"><p class="eyebrow">Seminar presentation · Full Read</p><h1>${escape(p.title)}</h1><p class="article-byline">${escape(p.author.name)}</p><p>${escape(event.title)} · 19 September 2026</p><div class="presentation-actions"><a class="button button-dark" href="#slide-1">Read presentation</a>${download}<a class="button button-outline" href="${asset(p.canonicalUrl)}" data-article-reader="${p.id}">Quick Read</a></div><p class="presentation-note">20 original slides in source order. Select a slide to enlarge it, read its selectable text, or inspect its original figures. The accompanying video appears with slide 3.</p></div></header>
<nav class="presentation-index" aria-label="Presentation slides">${source.pages.map(s=>`<a href="#slide-${s.page}" aria-label="Slide ${s.page}">${String(s.page).padStart(2,'0')}</a>`).join('')}</nav>
<div class="seo-article-body presentation-body">${source.pages.map(s=>`<section class="presentation-slide" id="slide-${s.page}"><h2>Slide ${String(s.page).padStart(2,'0')}</h2><figure><a href="${asset(s.image)}" target="_blank" rel="noopener noreferrer" aria-label="Enlarge original slide ${s.page}"><img src="${asset(s.image)}" alt="Original presentation slide ${s.page}; selectable text follows" width="${s.width}" height="${s.height}" loading="${s.page===1?'eager':'lazy'}"></a><figcaption>Original PDF · page ${s.page} of ${source.pages.length}</figcaption></figure>${s.page===3?`<figure class="presentation-video"><video controls playsinline preload="metadata" aria-label="Original presentation video accompanying PDF page 3"><source src="${asset(p.video)}" type="video/mp4">Your browser cannot play this video. <a href="${asset(p.video)}">Open original video</a></video><figcaption>Original presentation video · page 3. Use the player controls for sound and full-screen viewing.</figcaption></figure>`:''}<details class="presentation-transcript"><summary>Read selectable slide text</summary><pre>${escape(s.text).replace(/[ \t]+(?=\n)/g, whitespace => [...whitespace].map(c => c === " " ? "&#32;" : "&#9;").join(""))}</pre></details><details class="presentation-figures"><summary>Inspect original figures</summary><div>${s.figures.filter(f=>(repeated.get(f.src)||0)<10).map((f,i)=>`<a href="${asset(f.src)}" target="_blank" rel="noopener noreferrer" aria-label="Open original figure ${i+1} from slide ${s.page}"><img src="${asset(f.src)}" alt="Original embedded figure ${i+1} from PDF page ${s.page}" width="${f.width}" height="${f.height}" loading="lazy"></a>`).join('')}</div></details></section>`).join('')}</div><section class="article-page-tools">${download}<a class="button button-dark" href="../${event.detailUrl}">Return to seminar</a></section></article></main><footer class="seo-static-footer"><p>BA Medicale provides education, not individual diagnosis or treatment advice.</p><a href="../library.html">Medical Library</a><a href="../privacy-policy.html">Privacy Policy</a></footer><script src="../content.js?v=doctor-papers-20260901"></script><script src="../content-registry.js"></script><script src="../app.js?v=immersive-20260902"></script></body></html>\n`;
};
